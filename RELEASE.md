# Arcana 출시 절차서

오복사주 출시가 끝나면 이 문서를 위에서부터 따라가면 된다.
**🔴 표시는 사장님만 할 수 있는 일**이고, 나머지는 Claude 가 처리한다.

앱 ID `com.obok.arcana` / 작성일 2026-09-11

---

## 지금까지 끝난 것 ✅

| 항목 | 상태 |
|---|---|
| 앱 코드 (9개 국어 · 78장 · 5개 스프레드) | 완료 |
| 번역 검사 통과 (`npm run check:i18n`) | 완료 |
| 안드로이드 프로젝트 (`android/`) | 생성 완료 |
| 앱 아이콘 · 스플래시 136종 | 생성 완료 |
| 릴리스 서명 설정 (`build.gradle`) | 완료 |
| 깃허브 클라우드 빌드 2종 | 완료 |
| 개인정보처리방침 (영문) | 작성 완료 — `public/privacy.html` |
| 플레이 등록정보 원고 (영문) | 작성 완료 — `STORE.md` |
| 데이터 안전 · 콘텐츠 등급 답안 | 작성 완료 — `STORE.md` |
| 스토어 아이콘 512×512 | 완료 — `store/icon-512.png` |
| 그래픽 이미지 1024×500 | 완료 — `store/feature-graphic.png` |
| 휴대전화 스크린샷 5장 (1080×1920) | 완료 — `store/screenshots/` |

**남은 것은 전부 "계정에 로그인해서 등록하는 일"이다.** 코드 작업은 끝났다.

---

## 1단계 — 깃허브 저장소 만들기 🔴

클라우드 빌드를 쓰려면 코드가 깃허브에 있어야 한다. (이 PC 에는 Java 도 안드로이드
SDK 도 없다. 오복사주도 같은 방식으로 빌드했다.)

1. 🔴 github.com 에서 새 저장소 생성 — 이름 예: `arcana-tarot`
   - 오복사주 계정 그대로: `nobrain1988-bot`
   - **공개(Public) / 비공개(Private) 선택** — 아래 표 참고
2. 만들어진 주소를 Claude 에게 알려주면 코드 업로드까지 처리한다

| | 공개 (Public) | 비공개 (Private) |
|---|---|---|
| 클라우드 빌드 | 무료 무제한 | 월 2,000분 무료 (충분함) |
| 개인정보처리방침 호스팅 | 깃허브 페이지 무료로 가능 | 불가 (유료 플랜 필요) |
| 코드 공개 | 누구나 볼 수 있음 | 나만 봄 |

> 이 앱은 서버도 비밀키도 없어서 코드가 공개돼도 위험하지 않다.
> **공개로 하면 개인정보처리방침 호스팅이 공짜로 같이 해결된다.**

---

## 2단계 — 개인정보처리방침 주소 만들기 🔴

광고를 붙이므로 구글이 **필수**로 요구한다. 원고는 이미 `public/privacy.html` 에 있다.
셋 중 하나만 고르면 된다.

| 방법 | 비용 | 조건 | 결과 주소 |
|---|---|---|---|
| **깃허브 페이지** | 무료 | 저장소가 공개여야 함 (이미 공개다) | `nobrain1988-bot.github.io/arcana-tarot/privacy.html` |
| **Vercel** | 무료 | 오복사주에서 쓰던 방식 그대로 | `arcana-tarot.vercel.app/privacy.html` |
| 구글 사이트 도구 | 무료 | HTML 대신 글만 복사해 붙여넣기 | `sites.google.com/view/...` |

### 깃허브 페이지로 하는 법 (추천 — 5분, 파일은 이미 다 있다)

저장소 → **Settings** → 왼쪽 메뉴 **Pages** → 이렇게 고른다.

| 칸 | 고를 값 |
|---|---|
| Source | **Deploy from a branch** |
| Branch | **main** |
| 폴더 (Branch 옆 칸) | **/docs** |

그리고 **Save**. 1~2분 뒤 주소가 열린다.

- 개인정보처리방침: - 소개 페이지: 
> ⚠️ Source 를  로 고르면 안 된다. 이 저장소에는 Pages 용
> 워크플로가 없어서 배포될 것이 없고, 계속 404 가 난다.
>  폴더에  과  이 이미 들어 있으므로
> 그 폴더를 그대로 쓰는 쪽이 맞다.

---

## 3단계 — 애드몹 등록 🔴

오복사주와 **같은 애드몹 계정**(`pub-9311950226943560`)에 앱만 새로 추가하면 된다.

1. 🔴 애드몹 콘솔 → 앱 → 앱 추가 → Android → `com.obok.arcana`
2. 🔴 광고 단위 **2개** 생성
   - 배너 1개 (이름 예: `arcana-banner`)
   - 전면 1개 (이름 예: `arcana-interstitial`)
3. 🔴 아래 3개 값을 Claude 에게 전달하면 코드 교체는 Claude 가 한다

```
앱 ID      : ca-app-pub-9311950226943560~__________
배너 단위   : ca-app-pub-9311950226943560/__________
전면 단위   : ca-app-pub-9311950226943560/__________
```

> ⚠️ **앱 ID 가 틀리면 앱이 켜지자마자 죽는다.** 지금은 구글 테스트 값이 들어 있어서
> 테스트 빌드는 정상 동작하지만, 실광고로 바꾸지 않으면 수익이 0원이다.
> 교체 위치는 두 곳 — `android/app/src/main/AndroidManifest.xml`, `src/lib/ads.js`.

---

## 4단계 — 서명 키 비밀번호 등록 🔴

앱의 인감도장(업로드 키)을 만들 때 쓸 비밀번호다. **한 번 정하면 앱이 살아있는 내내 쓴다.**

1. 🔴 깃허브 저장소 → Settings → Secrets and variables → Actions → New repository secret
2. 🔴 이름 `ARCANA_KEYSTORE_PASSWORD`, 값은 직접 정한 비밀번호 (영문+숫자+기호 12자 이상)
3. 🔴 **그 비밀번호를 반드시 따로 적어 보관할 것** — 잃어버리면 앱 업데이트가 영원히 막힌다

> 오복사주는 비밀번호가 워크플로 파일에 그대로 적혀 있다. 저장소가 공개면 누구나 본다.
> 타로는 Secret 방식으로 만들어 두었다. (오복사주도 나중에 같은 방식으로 옮기는 걸 권한다)

---

## 5단계 — 테스트 빌드로 폰에서 확인 🔴

1. 🔴 깃허브 → Actions 탭 → `Android APK 빌드 (테스트용)` → Run workflow
2. 🔴 끝나면 Artifacts 에서 `arcana-debug-apk` 내려받기
3. 🔴 폰에 옮겨 설치 → 실제로 눌러보기

**확인할 것:** 한국어가 어색하지 않은지, 카드 그림이 깨지지 않는지, 광고가 뜨는지,
9개 국어 전환이 되는지.

---

## 6단계 — 정식 빌드 (AAB) 🔴

1. 🔴 Actions → `Android AAB (플레이 업로드용)` → Run workflow
2. 첫 실행에서 업로드 키가 자동 생성된다
3. 🔴 로그에 출력된 base64 문자열을 복사 → Secret `ARCANA_KEYSTORE_BASE64` 로 저장
   - **이걸 해야 다음 빌드부터 같은 키를 쓴다.** 안 하면 다음에 다른 키가 생겨 업로드가 거부된다
4. 🔴 Artifacts 에서 `arcana-upload-keystore-DO-NOT-LOSE` 도 내려받아 따로 백업
5. 🔴 `arcana-release-aab` 를 내려받으면 그게 플레이에 올릴 파일이다

---

## 7단계 — 플레이 콘솔 등록 🔴

1. 🔴 앱 만들기 → 이름 `Arcana: Daily Tarot Reading` / 언어 English (US) / 앱 / 무료
2. 🔴 `STORE.md` 의 원고를 그대로 복사해 넣기
   - 짧은 설명 · 전체 설명 · 카테고리 · 태그
3. 🔴 그래픽 자료 업로드 — **파일은 전부 만들어 뒀다. 올리기만 하면 된다**
   - 아이콘 512×512 ← `store/icon-512.png`
   - 그래픽 이미지 1024×500 ← `store/feature-graphic.png`
   - 스크린샷 ← `store/screenshots/` 의 5장 전부
     (화면을 고친 뒤에는 `npm run shots` 한 줄로 다시 뽑는다)
4. 🔴 앱 콘텐츠 → 데이터 안전 → `STORE.md` 5번 표대로 입력
5. 🔴 콘텐츠 등급 설문 → `STORE.md` 6번 표대로
6. 🔴 개인정보처리방침 URL 입력 (2단계에서 만든 주소)
7. 🔴 AAB 업로드 → 비공개 테스트 트랙

---

## 8단계 — 비공개 테스트 12명 / 14일 🔴

구글 정책: 신규 개인 개발자 계정은 **테스터 12명이 14일 연속 참여**해야 정식 출시가 열린다.

- 🔴 오복사주 테스터를 그대로 재활용할 수 있다 (같은 계정이면 명단 공유 가능)
- 🔴 14일은 줄일 수 없다. **오복사주 테스트와 겹쳐서 동시에 돌리는 것이 유일한 단축법이다**

> 💡 오복사주가 아직 테스트 중이라면, 타로도 **지금 바로 비공개 테스트를 시작**하는 게
> 낫다. 두 앱의 14일이 병렬로 흐른다. 순차로 하면 4주, 병렬이면 2주다.

---

## 개발 명령어 모음

| 명령 | 하는 일 |
|---|---|
| `npm run dev` | 브라우저에서 개발 서버 |
| `npm run build` | 웹 빌드 (dist) |
| `npm run check:i18n` | 9개 국어 × 78장 번역 검사 |
| `npm run icons` | 아이콘·스플래시 재생성 |
| `npm run shots` | 플레이스토어 스크린샷 5장 (1080×1920) — 미리보기 서버가 떠 있어야 한다 |
| `npm run feature` | 플레이스토어 그래픽 이미지 1024×500 |
| `npm run android:sync` | 웹 빌드 → 안드로이드에 반영 |
| `npm run build:single` | 링크 공유용 단일 HTML |
