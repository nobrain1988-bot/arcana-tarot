# Arcana — 글로벌 타로 앱 기획서

작성일 2026-09-10 / 작업방 `D:\project\APP\tarot`

---

## 1. 한 줄 정의

> 전 세계 아무 나라 사람이나 설치해서, **오늘의 카드 한 장**을 뽑고
> 그 결과를 캡처해 SNS에 올리는 앱.

- 타겟: 전 세계 (1차 영어권 + 브라질·멕시코·인니·터키·인도 — 타로 검색량 상위국)
- 오복사주와의 관계: **코드 구조는 형제, 콘텐츠는 완전 별개**

## 2. 왜 타로인가 (3줄 요약)

1. **번역이 필요 없다** — 타로 78장은 전 세계에서 같은 의미로 통한다. 사주는 번역 자체가 불가능하다.
2. **그림 비용이 0원** — 라이더-웨이트(1909) 원본은 퍼블릭 도메인이다.
   작가 파멜라 콜먼 스미스가 1951년에 사망해 사후 70년이 지났다(EU·영국·한국 2022-01-01 만료,
   미국은 1909년 출판으로 이미 만료). 위키미디어 공용에서 라이선스 근거와 함께 받는다.
   ⚠️ 인터넷에 도는 타로 이미지 대부분은 **현대에 다시 칠한 판본**이라 새 저작권이 붙어 있다.
   겉보기로 구분이 안 되므로 반드시 `npm run cards` 로만 받을 것 (라이선스를 기계가 검증한다).
3. **검색 수요가 있다** — 사람들이 스토어에서 "tarot"을 직접 검색한다. 광고비 없이 유입된다.

## 3. 수익 구조 (v1 = 광고만)

| 광고 | 위치 | 상태 |
|---|---|---|
| 배너 | 하단 고정 | 구현 완료 (테스트 ID) |
| 전면 | 결과 보기 직전, 최소 70초 간격 | 구현 완료 (테스트 ID) |
| 리워드 | v2 — AI 심층 해석용 | 미구현 |

인앱결제·구독 **없음**. → 구글 심사 항목이 크게 줄고, 환불·구독관리 코드도 불필요.

## 4. 기능 (v1 = 구현 완료)

| # | 기능 | 설명 | 목적 |
|---|---|---|---|
| 1 | **Daily Card** | 하루 한 장. 날짜+기기값 시드로 고정 → 같은 날 다시 열어도 같은 카드 | **재방문** |
| 2 | Past · Present · Future | 3장 클래식 스프레드 | 핵심 콘텐츠 |
| 3 | Yes or No | 한 장으로 예/아니오 + 근거 | 가벼운 진입 |
| 4 | Love Reading | You / The other / Between you | 체류시간 |
| 5 | Work & Money | 현재 / 막힌 것 / 뚫는 법 | 체류시간 |
| 6 | **Share** | 카드명+키워드만 공유 (질문 내용은 절대 안 나감) | **바이럴** |
| 7 | Journal | 뽑은 기록 최근 60개 (localStorage) | 재방문 |
| 8 | ~~Library~~ | 78장 사전 — **탭에서 제거**(2026-09-15). 되살리려면 LibraryView 를 지운 커밋 참조 |
| 9 | **9개 국어** | 폰 언어 자동 감지 + 설정에서 변경 | **글로벌 도달** |
| 10 | 설정 | 언어 / 역방향 사용 여부 / 데이터 삭제 | 사용자 통제권 |

## 4-1. 다국어 (v1에 포함)

카드 해석까지 **전부** 번역했다. 버튼만 번역하고 정작 읽으러 온 내용이 영어면 의미가 없다.

| 항목 | 수량 |
|---|---|
| 지원 언어 | 9개 — 영어, 스페인어, 포르투갈어, 한국어, 일본어, 러시아어, 터키어, 프랑스어, 독일어 |
| 번역 덩어리 | 언어당 78장 × 정/역방향 × (키워드+본문) = 312개 |
| UI 문구 | 언어당 약 70개 |

**설계 세 가지가 핵심이다.**

1. **폰 언어 자동 감지** — 대부분의 사용자는 설정창을 안 연다. 첫 실행에 폰 언어를 보고 맞춘다.
   설정창은 "자동 감지가 틀렸을 때 고치는 용도"다. 그래서 어느 화면에서나 닿도록 우상단에 고정했다.
2. **고른 언어 하나만 내려받는다** — 9개 국어 카드 해석을 전부 앱에 넣으면 시작이 느려진다.
   언어별로 파일을 쪼개서(지연 로딩) 한 언어당 15~19KB만 추가된다.
3. **빠진 번역은 자동으로 영어** — 번역이 덜 된 상태로 출시해도 화면이 비거나 앱이 죽지 않는다.
   '터키어 80% + 영어 20%'는 서비스가 되지만 빈 화면은 서비스가 안 된다.

카드 이름 조립은 언어마다 문법이 다르다. 각 언어 파일이 조립 방법을 직접 들고 있다.

| 언어 | Five of Swords | 문법 |
|---|---|---|
| 한국어 | 소드 5 | 수트 + 숫자 |
| 일본어 | ソードの5 | 조사 の |
| 독일어 | Fünf der Schwerter | 2격(속격) |
| 러시아어 | Пятёрка Мечей | 생격 복수 (목록에서는 주격 Мечи) |
| 터키어 | Kılıç Beşlisi | 단수 수트 + 소유격 어미 (목록에서는 복수 Kılıçlar) |

**검사 도구:** `npm run check:i18n` — 9개 국어 × 78장을 눈으로 볼 수 없으므로 기계가 본다.
빠진 항목, 구분자 오류, 없는 카드 id, 그리고 **다른 언어 글자가 섞인 것**까지 잡는다.
(실제로 일본어 본문에 `analyze`가, 러시아어 키워드에 `新`이 남아 있던 것을 이 도구가 잡아냈다)

## 5. 해석 엔진 — 지어내지 않는다

오복사주의 `[[saju-must-be-scholarly]]` 원칙을 그대로 적용했다.
문장을 랜덤으로 고르지 않고, **뽑힌 카드에서 계산되는 값만으로** 결론을 만든다.

| 계산되는 값 | 쓰이는 곳 |
|---|---|
| 카드 78 × 정/역방향 2 | 본문 (라이더-웨이트 전통 해석) |
| 자리(position)별 lead | "이 자리는 무엇을 말하는가" |
| 메이저 아르카나 장수 | "일상 규모인가 / 큰 국면인가" |
| 역방향 장수 | "밖으로 흐르는가 / 안에서 막혔는가" |
| 원소 분포 (불·물·공기·흙) | "이 리딩은 무엇에 관한 것인가" |
| 카드별 예/아니오 극성표 | Yes or No 판정 |

예/아니오 극성은 전통 배정을 표로 박아뒀다 (`src/lib/reading.js`). 주사위를 굴리지 않는다.

## 6. 기술 스택 (오복사주와 동일 — 새로 배울 것 없음)

| 항목 | 선택 | 오복사주 대비 |
|---|---|---|
| 프레임워크 | React 18 + Vite | 동일 |
| 앱 포장 | Capacitor 8 (Android) | 동일 |
| 광고 | @capacitor-community/admob | 동일 (`ads.js` 재사용) |
| 서버 | **없음** | Supabase 제거 |
| 결제 | **없음** | PortOne 제거 (한국 전용이라 글로벌엔 못 씀) |
| 저장 | localStorage | 로그인 불필요 |

**서버가 없다 = 월 고정비 0원.** 사용자가 100만 명이 와도 서버비가 안 나온다.

## 7. 파일 구조

```
src/
  lib/
    majors.js    메이저 22장 데이터 (영어 원문 = 번역의 기준)
    minors.js    마이너 56장 데이터 (4수트 × 14랭크)
    deck.js      78장 조립 + 시드 셔플 + 카드 뽑기
    topics.js    주제별 질문 (분류 4개 × 질문 15개). 스프레드 이름 대신 궁금한 내용으로 고른다
    spreads.js   스프레드 6종 '구조' (글자는 언어 팩에 있다)
    reading.js   해석 엔진 (예/아니오 극성표 포함, 글자 없음)
    ads.js       애드몹
    storage.js   localStorage (오늘의 카드·저널·설정)
  i18n/
    langs.js     지원 언어 목록 + 폰 언어 감지
    index.js     언어 팩 불러오기 + 영어로 자동 대체
    context.jsx  앱 전체에 현재 언어 공급 (useLang)
    ui/*.js      9개 국어 화면 문구 + 엔진 문장
    cards/*.js   9개 국어 카드 78장 해석
  components/
    Intro.jsx      시작 화면 — 문을 열고 들어가는 순간 (public/intro/reader.png 자리)
    Ambience.jsx   배경에 천천히 깜빡이는 별
    ChooseCards.jsx 부채꼴로 펼친 카드에서 직접 고르기
    CardArt.jsx    카드 앞면(라이더-웨이트 사진) + 뒷면. 사진 실패 시 자체 SVG 로 대체
    ResultView.jsx 결과 화면
    Settings.jsx   설정 시트 (언어·역방향·데이터)
    icons.jsx      탭바 아이콘
  App.jsx        3개 탭(오늘·리딩·기록) + 리딩 플로우
  styles.css     다크 + 골드 디자인
tools/
  check-i18n.mjs    번역 검사기 (npm run check:i18n)
  build-single.mjs  링크 공유용 단일 파일 빌드 (npm run build:single)
  fetch-card-art.mjs 카드 사진 78장 받기 + 라이선스 검증 (npm run cards)
  make-icons.mjs    앱 아이콘·스플래시 생성 (npm run icons)
```

**중요한 원칙:** `reading.js` 와 `spreads.js` 에는 글자가 한 자도 없다. 계산만 한다.
글자가 저기 섞이면 언어를 바꿔도 영어가 남는다. 새 문구는 반드시 `i18n/ui/*.js` 에 넣을 것.

## 8. 출시 전 체크리스트

**절차 전문은 `RELEASE.md` 에 있다.** 등록정보 원고는 `STORE.md`.

코드·문서 쪽 (끝남):

- [x] 앱 이름 최종 확정 — `Arcana: Daily Tarot Reading` (스토어명 27자)
- [x] 앱 아이콘 제작 — `tools/make-icons.mjs` 로 생성, 안드로이드 136종 반영
- [x] 안드로이드 프로젝트 생성 + 릴리스 서명 설정
- [x] 깃허브 클라우드 빌드 2종 (APK 테스트용 / AAB 플레이용)
- [x] 개인정보처리방침 영문 작성 — `public/privacy.html`
- [x] 스토어 등록정보 영문 작성 — `STORE.md` (ASO: tarot, tarot reading, daily tarot, tarot cards, free tarot)
- [x] 데이터 안전 · 콘텐츠 등급 답안 정리 — `STORE.md`

계정에 등록하는 쪽 (사장님):

- [ ] 깃허브 저장소 생성 → 코드 업로드
- [ ] 개인정보처리방침 호스팅 (깃허브 페이지 / Vercel / 구글 사이트 중 택1)
- [ ] AdMob에 새 앱 등록 → 광고 단위 2개 발급 → `src/lib/ads.js` + `AndroidManifest.xml` 교체
      (지금은 구글 테스트 ID. 앱 ID가 안 맞으면 앱이 즉시 죽는다)
- [ ] 서명 키 비밀번호를 깃허브 Secret 에 등록 (`ARCANA_KEYSTORE_PASSWORD`)
- [ ] 스크린샷 2장 이상 + 그래픽 이미지 1024×500
- [ ] 비공개 테스트 12명 / 14일 (오복사주 테스터 재활용 가능 — **두 앱 동시 진행 권장**)

## 9. 안 하는 것 (v1에서 의도적으로 제외)

- 로그인 / 회원가입
- 인앱결제 / 구독
- 서버 / DB
- iOS

## 10. 경쟁 서비스 분석 (2026-09 조사)

국내·해외 타로 앱/사이트를 조사한 결과. **국내와 해외가 정반대 방향을 강조한다.**

| | 해외 (Biddy Tarot · Labyrinthos · Galaxy Tarot) | 국내 (헬로우봇 · The 타로카드 · 오즈의 타로) |
|---|---|---|
| 조직 방식 | **스프레드 이름**으로 나눈다 (Past·Present·Future) | **상황 카테고리**로 나눈다 (애정운·금전운·취업운) |
| 연애 리딩 | 관계 하나를 3장으로 | **관계 상태별로 쪼갠다** — 솔로 / 짝사랑 / 썸 / 연애 중 |
| 가장 강조하는 것 | **질문 던지는 법** | 즉답·판정, 시기(언제) |
| 학습 | 카드 뜻 공부 과정 제공 | 거의 없음 |

**해외가 공통으로 가르치는 질문 원칙** (이번에 앱에 반영):
- `언제` `~될까요` `~해야 하나요` 로 시작하면 닫힌 질문 → 짐작밖에 안 나온다
- `무엇을` `어떻게` 로 물어야 쓸 수 있는 답이 나온다
- 연애 질문은 **상대를 엿보는 질문**(그 사람이 뭘 숨기나)이 아니라 **내 경험을 묻는 질문**으로
- 조종을 구하는 질문(어떻게 하면 날 사랑하게), 정확한 시점(언제 결혼) 은 피한다

### 반영한 것
- [x] 질문 화면에 원칙 한 줄 + **눌러서 입력되는 예시 3개** (9개 국어). 설명만 적으면 안 읽는다.
- [x] **상황 · 행동 · 결과 스프레드 추가** — Biddy 가 꼽는 기본 5종 중 하나였는데 빠져 있었다.
      다른 스프레드에 없는 '결과' 자리가 핵심이다. 주제별 질문 중 '이 마음 어떻게 해야 하나요',
      '이번 일은 어떻게 풀릴까요' 를 이 스프레드로 연결했다.
- [x] **주제별 질문으로 고르는 화면** (`src/lib/topics.js`). 분류 4개 × 질문 15개.
      스프레드 이름은 타로를 아는 사람의 언어다. 처음 온 사람은 `과거·현재·미래` 를 보고
      고를 근거가 없어서 거기서 멈춘다. 질문을 고르면 스프레드는 앱이 정하고 질문칸도 건너뛴다.
      스프레드를 직접 고르는 기존 화면은 `직접 물어보기` 로 남겨 뒀다.

### 아직 안 한 것 (판단 필요)

- [ ] **Blessings · Challenges · Action** — 역시 기본 5종. 가진 자원부터 보는 구성.
- [ ] 연애 리딩을 **관계 상태별로 분기** (솔로/짝사랑/썸/연애 중) — 국내 1위 앱의 방식.
      한국·일본 전환율에 영향이 클 수 있으나, 스프레드가 4배로 늘어 번역량도 4배가 된다.
- [ ] 시기(언제) — 수요는 크지만 타로로 시점을 짚는 건 전통적 근거가 약하다.
      `[[해석 엔진은 지어내지 않는다]]` 원칙과 충돌하므로 v1에서는 의도적으로 뺐다.

> 우리 `일과 돈` 스프레드(지금 / 막힌 것 / 뚫는 법)는 사실상 Situation·Challenge·Action 이고,
> 이건 해외에서도 표준으로 인정되는 구성이다. 이름만 직업용으로 좁혀 놓았을 뿐이다.

## 11. v2 후보

1. **AI 심층 해석** — 리워드 광고 시청 → 사용자가 쓴 질문을 그대로 넣어 AI가 개인화 해석.
   사용자가 자기 언어로 쓰면 AI가 그 언어로 답한다.
2. 카드 조합 해석 (2장 사이의 관계)
3. Celtic Cross 10장 스프레드
4. 알림 — 매일 아침 "오늘의 카드가 준비됐습니다"
5. 언어 추가 — 구조가 이미 잡혀 있어서 `ui/xx.js` + `cards/xx.js` 두 파일만 더하면 된다.
   후보: 아랍어(우→좌 배치 작업 필요), 인도네시아어, 힌디어, 이탈리아어, 폴란드어
